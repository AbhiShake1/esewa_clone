import 'package:flutter/material.dart';

class CheckoutPage extends StatelessWidget {
  const CheckoutPage({Key? key, required this.qrResult, required this.amount})
      : super(key: key);

  final Map qrResult;
  final double amount;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
